// empty bcs suitable for type --require

// some random code before the require variable
function add(x, y) {
  return x + y;
}

var require= {};


// some random code after the require variable
function sub(x, y) {
  return x - y;
}
