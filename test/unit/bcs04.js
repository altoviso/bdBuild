// empty bcs suitable for type --loader

// some random code before the require variable
function add(x, y) {
  return x + y;
}

require({});


// some random code after the require variable
function sub(x, y) {
  return x - y;
}
